import React from 'react';

const PageFour = props => (
	<div className="page">
		<pre style={{"white-space":"pre-line", "margin-bottom":"20px"}}>
		Unfortunately, you are not eligible to vote.</pre>
	</div>
);

export default PageFour;
